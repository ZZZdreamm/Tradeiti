server:
  port: 8090

usos:
    consumer:
      key: ${CONSUMER_KEY}
      secret: ${CONSUMER_SECRET}

jwt:
  key: ${JWT_KEY}

app:
  url:
    backend: ${BACKEND_URL}
    frontend: ${FRONTEND_URL}

springdoc:
  api-docs:
    path: /api/docs
  swagger-ui:
    path: /api/docs/ui